<BusinessLogic methodName="refreshCallSummaryReport" businessObjectClass="BoCallReports" businessObjectType="businessobject" asynchronous="false" accessibility="PUBLIC" final="false" module="CORE" simpleEditorOnly="">
  <Parameters>
    <MethodInput name="containerName" type="String" />
  </Parameters>
<Code language="JavaScript">
<![CDATA[var chartHelper = this.getChartHelper();
var chartString = "chart_" + containerName;
var chartSelector = "#"+chartString;

var d3Container = chartHelper.getUIContainer(containerName);
var chart = me.getChartHelper().getChartFromStorage(containerName); 

// Redraw chart title after orientation change by just drawing the title again
chartHelper.setChartTitle(d3Container, Localization.resolve("Report_CallSummary_Title"),containerName);

var sizeJson = chartHelper.computeContainerSize(containerName, 0.60, 0.96);
chart.resize(sizeJson);
var svg = d3.select(chartSelector);
var text = svg.selectAll("text");
text.style("font-size", "1.4em");
text.style("font-family", "Helvetica Neue, Roboto, Segoe UI, sans-serif");

setTimeout(function () {
  // center vertically
  me.centerVerticallyCallSummaryReport(containerName);
}, 100);]]>
</Code>
  <Return name="" value="" />
</BusinessLogic>