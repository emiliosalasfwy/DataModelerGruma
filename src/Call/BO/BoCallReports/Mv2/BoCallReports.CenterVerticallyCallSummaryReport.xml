<BusinessLogic methodName="centerVerticallyCallSummaryReport" businessObjectClass="BoCallReports" businessObjectType="businessobject" asynchronous="false" accessibility="PUBLIC" final="false" module="CORE" simpleEditorOnly="">
  <Parameters>
    <MethodInput name="containerName" type="String" />
  </Parameters>
  <Code language="JavaScript"><![CDATA[  var d3Container = this.getChartHelper().getUIContainer(containerName);

  var colorContDiv = d3Container.selectAll('#colorContainer');
  var titleDiv = d3Container.selectAll('#titleDiv');
  var mainDiv = d3Container.selectAll("#chart_" + containerName);

  if (Utils.isDefined(colorContDiv) && Utils.isDefined(titleDiv) && Utils.isDefined(mainDiv)) {
    if (Utils.isDefined(colorContDiv.node()) && Utils.isDefined(titleDiv.node()) && Utils.isDefined(mainDiv.node())) {
      
      var completeHeight = colorContDiv.node().getBoundingClientRect().height;
      var completeWidth = colorContDiv.node().getBoundingClientRect().width;
      var titleHeight = titleDiv.node().getBoundingClientRect().height;
      var mainHeight = mainDiv.node().getBoundingClientRect().height;

	  // If the topValue is 0 or not defined, then set a default to 100 
	  // This is impacting only the phones or tables with resolution less than 768 * 1024
	  var topValue = (completeHeight + titleHeight - mainHeight)/2;
      if(! Utils.isDefined(topValue) || topValue === 0){
 		topValue = 100;
      }

      mainDiv.style("position", "absolute").style("left", "0px").style("top", (topValue + "px"));
    }
  }]]></Code>
  <Return name="" value="" />
</BusinessLogic>