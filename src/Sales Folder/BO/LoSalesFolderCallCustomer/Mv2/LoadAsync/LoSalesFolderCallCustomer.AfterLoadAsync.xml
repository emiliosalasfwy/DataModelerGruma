<BusinessLogic methodName="afterLoadAsync" businessObjectClass="LoSalesFolderCallCustomer" businessObjectType="{BOType}" asynchronous="true" accessibility="PUBLIC" final="false" module="CORE" simpleEditorOnly="">
  <Parameters>
    <MethodInput name="result" type="Object" />
    <MethodInput name="context" type="Object" />
  </Parameters>
<Code language="JavaScript">
<![CDATA[var items = me.getAllItems();
var length = items.length;

if(length > 0) {
  for(var i = 0; i < length; i++){
    items[i].dateFrom = Localization.localize(items[i].dateFrom, "date");
    items[i].dateThru = Localization.localize(items[i].dateThru, "date");
  }
}

var promise = when.resolve(result);]]>
</Code>
  <Return name="result" value="promise" />
</BusinessLogic>