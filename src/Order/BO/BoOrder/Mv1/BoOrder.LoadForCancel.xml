<BusinessLogic methodName="loadForCancel" businessObjectClass="BoOrder" businessObjectType="businessobject" asynchronous="true" accessibility="PUBLIC" final="false" module="CORE" simpleEditorOnly="">
  <Parameters>
    <MethodInput name="jsonQuery" type="Object" />
  </Parameters>
<Code language="JavaScript">
<![CDATA[var promise = Facade.getObjectAsync(BO_ORDER, jsonQuery)
  .then(function (selfJson) {
  me.setProperties(selfJson);
  me.setIsCancel("1");
  var jsonParams = me.prepareLookupsLoadParams(selfJson);
  return Facade.loadLookupsAsync(jsonParams);
})
  .then(function (lookups) {
  me.assignLookups(lookups);

  return BoFactory.loadObjectByParamsAsync(BO_ORDERMETA, me.getQueryBy("pKey", me.getSdoMetaPKey()));
})
  .then(function (boOrderMeta) {
  me.setBoOrderMeta(boOrderMeta);

  return BoFactory.loadObjectByParamsAsync(LO_ORDERNOTES, me.getQueryBy("sdoMainPKey", me.getPKey()));
})
  .then(function (loNotes) {
  me.setLoNotes(loNotes);
  return me;
});]]>
</Code>
  <Return name="promise" value="promise" />
</BusinessLogic>