<BusinessLogic methodName="getSignatureApprovalVisible" businessObjectClass="BoSysReleaseProcess" businessObjectType="businessobject" asynchronous="false" accessibility="PUBLIC" final="false" module="CORE" simpleEditorOnly="">
  <Parameters>
    <MethodInput name="groupName" type="String" />
  </Parameters>
<Code language="JavaScript">
<![CDATA[var steps = me.getLoSysReleaseProcessStep().getAllItems();
var length = steps.length;
var visible = false;

if (me.getReadOnlyMode() === "0") {
  switch(groupName) {
    case "Group1":
      visible = (length > 0 && (steps[0].getAuthentication() == "LoginWithPIN" || steps[0].getAuthentication() == "LoginPINBarcode"));
      break;

    case "Group2":
      visible = (length > 1 && (steps[1].getAuthentication() == "LoginWithPIN" || steps[1].getAuthentication() == "LoginPINBarcode"));
      break;

    case "Group3":
      visible = (length > 2 && (steps[2].getAuthentication() == "LoginWithPIN" || steps[2].getAuthentication() == "LoginPINBarcode"));
      break;

    case "Group4":
      visible = (length > 3 && (steps[3].getAuthentication() == "LoginWithPIN" || steps[3].getAuthentication() == "LoginPINBarcode"));
      break;
  }
}]]>
</Code>
  <Return name="visible" value="visible" />
</BusinessLogic>