<BusinessLogic methodName="scanBarcode" businessObjectClass="LoScannedKPIProducts" businessObjectType="listobject" asynchronous="false" accessibility="PUBLIC" final="false" module="CORE" simpleEditorOnly="">
  <Parameters />
<Code language="JavaScript">
<![CDATA[var promise = Facade.getBarCodeScannerDataAsync().then(
  function (result) {
    Utils.hideEventLoadMask();
    if (!result.cancelled) {
      if (Utils.isEmptyString(result.text)) {
        result.text = "cancelled";
      }
      if (result.text !== "" || result.text !== undefined || result.text !== null) {
        return (result.text).trim();
      } else {
        return result.text;
      }
    }
    else {
      return "cancelled";
    }
  });]]>
</Code>
  <Return name="value" value="promise" />
</BusinessLogic>